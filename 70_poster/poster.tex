\documentclass[a0, landscape, final]{./unirostock/a0poster}
\usepackage[english]{babel}
\usepackage{helvet}
\usepackage{amsmath}
\usepackage{url}
\usepackage[normalem]{ulem}
\usepackage{setspace}
\usepackage[dvipsnames, table]{xcolor}
\usepackage[breaklinks=true]{hyperref}
\usepackage{lipsum}
    \hypersetup{colorlinks, citecolor=BrickRed, filecolor=Black, linkcolor=Black, urlcolor=MidnightBlue}
\usepackage[backend=biber,bibencoding=utf8,urldateusetime=true,style=ieee,sorting=none,natbib=true, minbibnames=1, maxbibnames=2]{biblatex}
\addbibresource{./refs.bib}

\usepackage[uni]{./unirostock/unirostock}
% uni, inf, msf, ief, mnf, mef, juf, wsf, auf, thf, phf

\usepackage{caption}
\captionsetup{
  font=normalsize, 
  labelfont=bf, 
  labelsep=period,
  figurename=Fig.,
  tablename=Tab.
}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{caption}

\title{Oil Slick Detection in Sentinel-1 Data}
\subtitle{\,Using a CNN Baseline \& the Geospatial Foundation Model Terramind}

\columncount{3} % Spaltenzahl im Fliesstext

\addcontributor{Malte Grube}{}
\addcontributor{Suma Gonuguntla}{}
\addcontributor{Vishnu Kaivalya Gudapati \& \uline{Ayush Prasad}}{}

\addinstitute{\hspace{-.2em}}{Institute of Computer Science}{}{}{University of Rostock}{A-Einstein-Str. 22}{Rostock}{Germany}

% \renewcommand{\mylogo}{\includegraphics[width=110mm]{IFI.png}}

\AtBeginBibliography{\normalsize}

\begin{document} 

% todo: watch this here below for figures
% how to add figures (should be like this):
% \myfigure{<column>}{<path>}{<img-scale>}{<label for reference>}{<caption>}
% \myfigure{0}{./img/image.png}{1.0}{fig:motivation}{}

\nocite{11445875}

\linespread{1.15}

\section{Introduction} \vspace{-1em}
\subsection{Project Motivation} \vspace{-1.5em}

\begin{itemize}
  \item \textbf{Oil slicks} $\to$ severe damage \& hard to monitor manually
  \item \textbf{Sentinel-1 SAR imagery} $\to$ day / night / all-weather monitoring possible
  \item \textbf{Short surface waves} $\to$ slicks as dark patches in SAR imagery
\end{itemize}
\vspace{.5em}
\subsection{Research Questions} \vspace{-1.5em}
\begin{itemize}
  \item \textbf{RQ1:} How well can deep learning detect oil slicks in \textit{Sentinel-1 SAR imagery}, and how does performance change between an in-distribution random split and a geographic out-of-distribution split (\textit{Mediterranean})?
  \item \textbf{RQ2:} Can a \textit{Geospatial Foundation Model} improve over a CNN baseline, and what do XAI methods reveal about the features that drive the predictions?
\end{itemize}

\section{Methodology} \vspace{.5em}
\subsection{Experimental Setup} \vspace{-1.5em}
\begin{itemize}
  \item \textbf{CNN baseline} $\to$ trained from scratch on OilSlick
  \item \textbf{Terramind} \cite{11445875} $\to$ frozen backbone, only training a classification head
  \item \textbf{Grid search} $\to$ performed for both models (36 total training runs)
  \item \textbf{Find best configuration} $\to$ based on val loss for each model \& split
  \item \textbf{Final evaluation} $\to$ 4 final models $\to$ individual test set
\end{itemize}
\vspace{.85em}
\subsection{Crucial Steps before Training} \vspace{-1.5em}
\begin{itemize}
  \item \textbf{Data analysis} $\to$ understand data distributions \& apply imagery filtering
  \item \textbf{Data filtering} $\to$ find corrupt TIFF files \& imagery with >= 40\% of nodata
\end{itemize}

\myfigure{0}{filtering.png}{0.99}{}{Filtering of OilSlick's Sentinel-1 data subset. The blue bars represent the number of images before filtering, while the orange bars represent the number of images after filtering.}

\columnbreak
\section{Results}
\myfigure{0}{./val_loss_gfm_random.png}{0.99}{}{Example of the grid search validation loss curves for the \textbf{Terramind} model (\textit{Random} split) -- to find the best configuration for evaluation based on the lowest validation loss.}

\vspace{.5em}
\subsection{Test Set-based Evaluation}

\myfigure{0}{../50_evaluation/combined_confusion_matrices.png}{0.99}{}{Confusion matrices for the individual test sets across random \& geographic splits for each final model -- obtained from the 4 grid search runs.}

\columnbreak

\subsection{Example Failure Cases} \vspace{-1em}

\myfigure{0}{../50_evaluation/random\_split/cnn/results/failure\_analysis/false\_negatives/fn\_251.png}{0.79}{}{Example SAR chip from the \textit{Random} test set, classified as \textit{No Slick} by the CNN baseline, which is a \textbf{False Negative (FN)}.} \vspace{-.3em}

\myfigure{0}{../50\_evaluation/geographic\_split/gfm/results/failure\_analysis/false\_positives/fp_18.png}{0.79}{}{Example SAR chip from the \textit{Geographic} test set, classified as \textit{Slick} by the Terramind model, which is a \textbf{False Positive (FP)}.}

\section{Conclusion \& Future Work}
\begin{itemize}
  \item \textbf{RQ1:} Oil Slick F1 scores $\sim$\textbf{0.71--0.77}, only mild OOD drop ($\sim$\textbf{0.03--0.04}) \vspace{.5em} % surprisingly robust across regions (see F1 score column for Oil Slick (geo cnn vs random cnn / geo gfm vs random gfm)). so, ML/DL can be used to detect oil slicks in SAR imagery across several regions
  \item \textbf{RQ2:} GFM $\to$ higher Slick F1 ($\uparrow$ \textbf{0.03}), lower No-Slick F1 ($\downarrow$ \textbf{0.07}) \vspace{.3em}\\$\to$ \textit{no clear improvement over CNN} $\mid$ XAI not applied (time) \vspace{.4em} % see GFM random and CNN random; GFM geo and CNN geo even differ less
  \item \textbf{Limitations:} All models trade precision for recall (Oil Slick) % recall > precision for each config; random CNN is more balanced and shows highest Slick precision with 0.7285, but lowest Slick recall with 0.7586 --> F1=0.7432
   $\to$ \textit{False alarms} on lookalikes as key failure mode $\mid$ Dataset quality \textit{(invalid samples)} \vspace{.4em} % even after filtering <= -50db imagery / >= 40% nodata / std=0.0 imagery (corrupt)
  \item \textbf{Future Work:} XAI-driven failure analysis to \textit{interpret} model behavior on lookalikes, and experimenting on (larger) SAR datasets with less invalid samples % since we are no domain-specific experts, we cannot tell why invalid samples still remain in the dataset after filtering
\end{itemize}

\printbibliography

\end{document}

