## TODO

- [ ] really ensure all imagery data is downloaded / available on HF somewhen
- [ ] ensure that all data are useful / find non-useful data
- [ ] load VV and VH as a tensor properly, normalized via zscore/overall normalization??
- [ ] do mean-std-normalization/zscore in EDA notebook and python scripts for training??
- [ ] finish EDA notebook

---

- [ ] add first initial training script
- [ ] think about regularization techniques (early stopping, L2 reg using adam or adamW, data augmentation, dropout [done])
- [ ] set up logging via mlflow during training, validation and testing; after this, we can train properly for both splits
- [ ] fine-tune resnet50 (changed input dims to 2; and a proper head for binary classification)
- [ ] fine-tune terramind-small with a proper binary head as well
- [ ] measure metrics: acc, pre, rec, f1, auc-roc

---

- [ ] maybe: do it for all images, 500 images and 100 images (might be a bit optional, since we have properly set up dataset splits by supervisor)
