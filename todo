## TODO

- move filtered_metadata.csv and split_stats.json to 20_data_analysis/ and read them from there in the training pipeline
- add class weighting based on the imbalance (positive vs. negative) in the training set for both splits
- setup proper he kaiming init for baseline model instead of default pytorch init
- for both models: use warmup phase and cosine annealing for learning rate scheduling
- add early stopping for training (monitoring validation loss)
- we need to do grid search for finding the best hyperparameters for both models (using learning rate, weight decay)
- add mlflow for logging of training, validation and test metrics; also log hyperparameters for each run (saved in \*split/cnn/logs/ and \*split/gfm/logs/)
- add saving of model checkpoints during training (best val loss) and after training (final model) for each config (saved in \*split/cnn/models/ and \*split/gfm/models/)
- the \*split/*/results/ are used for eval.py, failure_analysis.py (confusion matrices, lookalike analysis) and Grad-CAM for CNN

- [x] really ensure all imagery data is downloaded / available on HF somewhen
- [?] ensure that all data are useful / find non-useful data via thresholding (still need to identify more invalid samples and reasons for that)
- [x] load VV and VH as a tensor properly, normalized via zscore/overall normalization in TRAINING PIPELINE
- [x] do mean-std-normalization/zscore in EDA notebook
- [x] overall finish of EDA notebook

---

- [ ] instead of resnet50, small cnn baseline --> we might have to change filtering based on CNN results/failure cases
- [x] calculate train set means/stds for both splits (random and geographic)
- [x] fix the use of these values (either random or geographic) in the training/dataloader pipeline depending on the split for training
- [x] somewhere, we need to inject information about the filtered data splits (which images are not used for train/val/test anymore?)
- [x] add first initial training script
- [ ] think about regularization techniques
  - [ ] early stopping
  - [x] L2 reg using adam or adamW
  - [x] data augmentation
  - [x] dropout layer in classification head
- [ ] after initial train setup, include logging via mlflow during training, validation and testing; after this, we can train properly for both splits
- [ ] fine-tune (Ayush says "from scratch"?) resnet50 (changed input dims to 2; and a proper head for binary classification)
- [ ] fine-tune terramind-small with a proper binary head as well
- [x] measure metrics: acc, pre, rec, f1, auc-roc

---

- [ ] analyze failure cases/lookalikes via confusion matrix and example chips
- [ ] XAI: Grad-CAM for CNN-based model
